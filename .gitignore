node_modules/
auth_info/
*.log
.env
# Local data (DATA_STORE=local) — client lists are personal data, never commit them.
# The leading / matters: without it, src/data/ (source code) is ignored too.
/data/
/data-demo/
# Firebase service-account keys are passwords to the whole project
*firebase-adminsdk*.json
